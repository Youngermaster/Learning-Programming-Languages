#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
    int iterator;
    iterator = 0;
    while (iterator <= 10)
    {
        cout<<"Iterator = "<<iterator<<endl;
        iterator++;
    }
    return 0;
}
